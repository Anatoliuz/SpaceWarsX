#include "commandmove.h"


/* NOTHING */


/*
template <class Receiver>
commandMove<Receiver>::commandMove(Receiver& r_, MoveAction a)
{
    baseCommand<Receiver>::r = r_;
    action = a;
} */



/*
// move id1 id2 number |end пробелы на нужных местах
bool commandMove::doCommand(vector<string> command)  // проверенео 20.04.16 работает
{
    assert (!command.empty());
    assert (command[0] == "move");

    int id1 = strtoInt(command[1]);
    int id2 = strtoInt(command[2]);
    int number = strtoInt(command[3]);

 //   calulationModule::Move(id1, id2, number);
}*/
